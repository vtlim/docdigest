[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "docdigest"
version = "0.1.0"
dependencies = [
    "markdown-analysis",
    "anthropic"
]

[project.scripts]
docdigest = "docdigest.main:main"

[tool.setuptools.packages.find]
where = ["."]
